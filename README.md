# Resume

My resume, built with [Typst](https://typst.app/).

## Preview

![Resume Preview](preview.png)

## Building

```bash
nix-shell -p typst --run 'typst compile resume.typ'
```

Or if you have Typst installed:

```bash
typst compile resume.typ
```
